// its actually GameBuilderKey, but for repository requirements, we are not gonna import it
// To start that project as standalone, we shouldn't need GameBuilderKey
export const MATCH_FIGHT_GAME_NAME = "MatchFight";

